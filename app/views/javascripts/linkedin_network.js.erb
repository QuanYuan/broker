
  var SettingsLinkedIn = Backbone.View.extend({
    template: '<%= erb(:"settingsLinkedIn.html").gsub(/([^>\r\n]?)(\r\n|\n\r|\r|\n)/," ") %>',
    className: "linkedInSettingsMenu",

    initialize: function () {
      _.bindAll(this);
      this.render();
    },

    render: function () {
      var body;
      body = _.template(this.template, {
      });
      this.$el.html(body);
    }
  });

